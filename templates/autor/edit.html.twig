{% extends 'base.html.twig' %}

{% block title %}Editar autor{% endblock %}

{% block body %}

<div class="mb-4">
    <h1 class="h3 mb-1">
        <i class="bi bi-pencil me-2"></i>
        Editar autor
    </h1>

    <p class="text-secondary mb-0">
        Atualize as informações do autor.
    </p>
</div>

{{ include('autor/_form.html.twig', {'button_label': 'Atualizar'}) }}

<div class="mt-3">
    {{ include('autor/_delete_form.html.twig') }}
</div>

{% endblock %}
